@model ChilliSoftAssessmentMeetingMinuteTakerDLL.ViewModels.CreateMeetingViewModel


<form asp-action="CreateNewMeeting" asp-controller="Meeting">
<div id="MeetingModal" class="modal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Create Meeting</h5>
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">

                <div class="form-group">
                    <label class="col-form-label">Meeting Name</label>
                        <input asp-for="MeetingName" id="Name" type="text" class="form-control">
                </div>

                    <div class="form-group">
                        <select asp-for="MeetingTypeId" style="width:90%">
                            @foreach (var item in Model.MeetingTypes)
                            {
                                
                            <option value="@item.Id">@item.MeetingTypeName</option>
                            }

                        </select>
                    </div>

              @*  <div class="form-group">
                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Meeting Type
                        </button>
                        <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                            <a class="dropdown-item" href="#">MANCO</a>
                            <a class="dropdown-item" href="#">Finance</a>
                            <a class="dropdown-item" href="#">Project Team Leaders</a>
                        </div>
                    </div>
                </div>*@

            </div>
            <div class="modal-footer">
                    <button id="CreateMeetingForm" type="submit" class="btn btn-primary">Submit</button>

                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
                </div>
        </div> @*type="submit"data-bs-save="modal"*@
    </div>
</form>

<script type="text/javascript">
    $("#CreateMeeting").click(function () {
        console.log("Created");
        $(location).attr('href', 'https://google.com');
    });
</script>